<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_marginLeft="5sp"
    android:layout_marginRight="5sp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/timeTxt"
        style="@style/card_text_style"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:textStyle="normal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="2 PM" />

    <ImageView
        android:id="@+id/statusHourlyImage"
        android:layout_width="50sp"
        android:layout_height="50sp"
        app:layout_constraintEnd_toEndOf="@+id/timeTxt"
        app:layout_constraintStart_toStartOf="@+id/timeTxt"
        app:layout_constraintTop_toBottomOf="@+id/timeTxt"
        tools:srcCompat="@drawable/m_sun" />

    <TextView
        android:id="@+id/tempTxt"
        style="@style/card_text_style"
        android:textStyle="normal"
        app:layout_constraintEnd_toEndOf="@+id/statusHourlyImage"
        app:layout_constraintStart_toStartOf="@+id/statusHourlyImage"
        app:layout_constraintTop_toBottomOf="@+id/statusHourlyImage"
        tools:text="25" />

</androidx.constraintlayout.widget.ConstraintLayout>